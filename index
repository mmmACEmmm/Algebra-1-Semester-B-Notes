<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Algebra 1, Semester B - Course Units and Materials">
    <title>Math Units - Algebra 1, Semester B</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        /* CSS Reset and Variables */
        :root {
            --primary-color: #ff5e57;
            --text-color: #ffffff;
            --background-dark: #1e1e1e;
            --unit-hover: #2a2a2a;
            --disabled-color: #666;
            --transition-speed: 0.3s;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Global Styles */
        body {
            font-family: 'Roboto', -apple-system, BlinkMacSystemFont, 'Segoe UI', Oxygen, Ubuntu, sans-serif;
            background: linear-gradient(135deg, #0f0c29, #302b63, #24243e);
            color: var(--text-color);
            min-height: 100vh;
            display: grid;
            place-items: center;
            padding: 2rem;
            line-height: 1.6;
        }

        /* Main Container */
        .container {
            max-width: 800px;
            width: 100%;
            text-align: center;
        }

        /* Header Styling */
        .header {
            margin-bottom: 2rem;
        }

        h1 {
            font-size: clamp(2rem, 5vw, 3rem);
            color: var(--primary-color);
            text-shadow: 2px 2px 5px rgba(255, 94, 87, 0.7);
            letter-spacing: 2px;
            margin-bottom: 1rem;
        }

        .semester-info {
            font-size: 1.2rem;
            opacity: 0.9;
            margin-bottom: 2rem;
        }

        /* Column Container */
        .column {
            background-color: rgba(0, 0, 0, 0.7);
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(8px);
            max-width: 500px;
            margin: 0 auto;
        }

        /* Unit Grid */
        .units-grid {
            display: grid;
            gap: 1.5rem;
        }

        /* Individual Unit Box */
        .unit {
            background: var(--background-dark);
            padding: 1.5rem;
            border-radius: 10px;
            transition: all var(--transition-speed) ease-in-out;
            position: relative;
            overflow: hidden;
        }

        .unit:not(.disabled) {
            cursor: pointer;
        }

        .unit:not(.disabled):hover {
            transform: translateY(-5px);
            background-color: var(--unit-hover);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.7);
        }

        /* Link Styling */
        .unit a {
            color: var(--primary-color);
            text-decoration: none;
            font-weight: bold;
            display: block;
            padding: 0.5rem;
            position: relative;
            z-index: 1;
        }

        /* Unit Icons */
        .unit i {
            margin-right: 0.5rem;
            font-size: 1.2em;
        }

        /* Progress Indicator */
        .progress-bar {
            height: 4px;
            background: rgba(255, 94, 87, 0.3);
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
        }

        .progress {
            height: 100%;
            background: var(--primary-color);
            width: 0%;
            transition: width var(--transition-speed) ease;
        }

        /* Disabled Units */
        .unit.disabled {
            background-color: rgba(51, 51, 51, 0.7);
            cursor: not-allowed;
            color: var(--disabled-color);
        }

        .unit.disabled:hover {
            transform: none;
            box-shadow: none;
        }

        .unit.disabled i {
            color: var(--disabled-color);
        }

        /* Lock Icon for Disabled Units */
        .lock-icon {
            position: absolute;
            right: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--disabled-color);
        }

        /* Responsive Design */
        @media (max-width: 600px) {
            .column {
                padding: 1rem;
            }

            .unit {
                padding: 1rem;
            }

            h1 {
                font-size: 2rem;
            }
        }

        /* Accessibility */
        @media (prefers-reduced-motion: reduce) {
            .unit {
                transition: none;
            }

            .unit:hover {
                transform: none;
            }
        }

        /* Focus States */
        .unit a:focus {
            outline: 2px solid var(--primary-color);
            outline-offset: 4px;
            border-radius: 4px;
        }

        /* Dark Mode Support */
        @media (prefers-color-scheme: dark) {
            :root {
                --background-dark: #1a1a1a;
                --unit-hover: #2d2d2d;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>Algebra 1, Semester B</h1>
            <p class="semester-info">Spring 2025</p>
        </header>
        
        <main class="column">
            <div class="units-grid">
                <div class="unit">
                    <a href="https://docs.google.com/presentation/u/0/d/1_0oSTazxLSwWwpV1hfY9BfZ_fbOUP3BxArrdA1Xzmxg/edit" target="_blank" rel="noopener">
                        <i class="fas fa-square-root-alt"></i>Unit 1: Polynomials
                    </a>
                    <div class="progress-bar">
                        <div class="progress" style="width: 100%"></div>
                    </div>
                </div>

                <div class="unit">
                    <a href="https://docs.google.com/presentation/d/1x88WlKnXGpjxpwxpK0c-6TIKOvUwom5VWRuZuCzQnfg/edit?usp=sharing" target="_blank" rel="noopener">
                        <i class="fas fa-chart-line"></i>Unit 2: Quadratic Relationships
                    </a>
                    <div class="progress-bar">
                        <div class="progress" style="width: 75%"></div>
                    </div>
                </div>

                <div class="unit">
                    <a href="https://docs.google.com/presentation/d/1fEfJa2goNhxfDJpMZDr0JnsUPjEymf_vULtj2M1RLb4/edit?usp=sharing" target="_blank" rel="noopener">
                        <i class="fas fa-superscript"></i>Unit 3: Solving Quadratic Equations
                    </a>
                    <div class="progress-bar">
                        <div class="progress" style="width: 30%"></div>
                    </div>
                </div>

                <div class="unit disabled">
                    <span>
                        <i class="fas fa-chart-exponential"></i>Unit 4: Exponential Relationships
                    </span>
                    <i class="fas fa-lock lock-icon"></i>
                </div>

                <div class="unit disabled">
                    <span>
                        <i class="fas fa-function"></i>Unit 5: Mixed Functions
                    </span>
                    <i class="fas fa-lock lock-icon"></i>
                </div>

                <div class="unit disabled">
                    <span>
                        <i class="fas fa-graduation-cap"></i>End of Semester Test: Algebra 1B
                    </span>
                    <i class="fas fa-lock lock-icon"></i>
                </div>
            </div>
        </main>
    </div>
</body>
</html>
